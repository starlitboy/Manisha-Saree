[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serves index.html for any path (handles hash links & direct URLs)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
